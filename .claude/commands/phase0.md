Execute Phase 0 from CLAUDE.md: verify the scaffold installs cleanly
(`pip install -e ".[dev]" --timeout 120 --retries 10`), pre-commit hooks pass,
and `pytest` runs green. Fix any config drift. Commit as "phase0: scaffold verified".
