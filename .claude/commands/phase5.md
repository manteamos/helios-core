Execute Phase 5 from CLAUDE.md (FastAPI + Celery + Docker).
202 Accepted pattern (NOT 222 — spec typo), prefork Celery workers, multi-stage
non-root Dockerfile from python:3.11-slim with no GUI libs. End with a full
end-to-end annual simulation through the queue using docker compose.
